{% extends "layouts/base.html.twig" %}

{% block title 'Modifier une adresse | ' ~ parent() %}

{% block content %}
    <div class="float-right mt-2 mr-2">
        <a href="{{path('account_addresses')}}" class="btn btn-secondary" title="mes adresses">
            <i class="fas fa-long-arrow-alt-left"></i> Mes adresses
        </a>
    </div>
    <div class="jumbotron">
        <h3 class="display-6 text-center">
            <i class="fas fa-map-marker-alt user-color"></i> 
             Modifier une adresse
        </h3>
        <br>
        {{ include ('account/_formAddress.html.twig', {form:form, button: ' Modifier'}) }}
    </div>
{% endblock %}
